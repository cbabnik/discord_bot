const { example } = require('../components/example');

const expect = require('chai').expect;
const sinon = require('sinon');

// stub saveJSON so nothing is written

// beforeEach set json to something to ensure predictability

describe("Example", () => {
    describe('+win', () => {
        it('should count 1st then 2nd', () => {

        });
        it('should not crash if the json file is empty', () => {

        });
        it('should track wins independently from different sources', () => {

        });
    });

    describe('+delay', () => {
        it('', () => {

        });
    });
    describe('+spam', () => {
        it('', () => {

        });
    });
    describe('+countdown', () => {
        it('', () => {

        });
    });
    describe('+alarm', () => {
        it('', () => {

        });
    });
});
